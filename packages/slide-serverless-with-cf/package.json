{
  "name": "serverless-with-cf",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "slidev build",
    "dev": "slidev --open",
    "export": "slidev export",
    "publish": "slidev build --base /$npm_package_name && cp -pR static/* dist && aws s3 cp dist --endpoint-url $S3_ENDPOINT s3://$S3_UPLOAD_BUCKET/$npm_package_name --recursive",
    "screenshot": "slidev export --format png --output picture  --wait 10000 && ls -l picture"
  },
  "dependencies": {
    "@slidev/cli": "^0.49.29",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "playwright-chromium": "^1.48.1",
    "vue": "^3.5.12"
  }
}
